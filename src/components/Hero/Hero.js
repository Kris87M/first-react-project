import PageTitle from '../PageTitle/PageTitle';

const Hero = () => {
    return (
      <PageTitle title='My first React App' subtitle='A simple to-do app, with lists, columns and card'/>
    );
  };

  export default Hero;